# Project Rules

## Debugging

- All debug/investigation scripts must be created inside a `debug/` folder, never in the project root.
- Once debugging is complete, delete the entire contents of `debug/` before finishing the task.
